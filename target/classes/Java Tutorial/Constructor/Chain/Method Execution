
//2)Using for method description connected to the object


public class Executor{

    public void settingValues(Cars car)

    Helper.setField(locator, car.model);
    Helper.setField(locator, car.doors);
}